CREATE INDEX idx_pi_payments_payment_intent
ON pi_payments(payment_intent_id);

CREATE INDEX idx_pi_payments_user
ON pi_payments(user_id);

CREATE INDEX idx_pi_payments_status
ON pi_payments(status);

CREATE INDEX idx_pi_payments_created
ON pi_payments(created_at DESC);
